# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Proprietary installer — do not ship in the open-source tree
setup.exe
*.exe

# Local data / secrets
.env
.env.*
!.env.example

# Build / tool caches (kept off root disk when needed)
.cache/

# Rust build targets
target/

# Local server data (JWT secret, registry, company databases)
kite-data/
*.db
*.db-journal
*.db-wal
*.db-shm

# TypeScript incremental build info
*.tsbuildinfo

# Demo recording artifacts (regenerate with npm run kite:demo)
demo/

# Installers (attach to GitHub Releases instead of committing)
installers/

# OS
Thumbs.db
